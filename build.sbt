name := "battleship"

version := "1.0"

scalaVersion := "2.11.8"

resolvers in ThisBuild += "Typesafe repository releases" at "http://repo.typesafe.com/typesafe/releases"

resolvers in ThisBuild += "Sonatype OSS Snapshots" at "https://oss.sonatype.org/content/repositories/releases/"

resolvers in ThisBuild += "Twitter Maven" at "https://maven.twttr.com"